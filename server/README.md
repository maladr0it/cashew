# Setup

## Prerequisite

1. install postgresql
   `brew install postgresql`

2. run postgres
   `brew services start postgresql`

3. install node
   `brew install node`

## start server

`npm install`
`npm run start:dev`
